# Sodium FPS Step Fix - Access Transformers (NeoForge)
# The Fabric build uses an access widener (see common/src/main/resources/sodiumfpsstepfix.accesswidener);
# NeoForge does not support access wideners, so the same visibility change is expressed here.
#
# OptionsMixin returns OptionInstance.IntRange (public) from a method typed
# OptionInstance.SliderableValueSet, which is package-private in vanilla 1.21.1.
public net.minecraft.client.OptionInstance$SliderableValueSet
