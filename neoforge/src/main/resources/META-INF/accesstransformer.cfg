# Sodium FPS Step Fix - Access Transformers (NeoForge)
# Fabric build uses an access widener; NeoForge does not support AW, so the
# same visibility change is expressed here. In MC 26.1.x these two nested
# interfaces are package-private (ValueSet and IntRange are already public):
public net.minecraft.client.OptionInstance$SliderableValueSet
public net.minecraft.client.OptionInstance$IntRangeBase
